version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - gosec
    - govet
    - ineffassign
    - staticcheck
    - unused
  settings:
    errcheck:
      exclude-functions:
        # Best-effort cleanup; logged elsewhere where it matters.
        - (*os.File).Close
        - fmt.Fprintln
        - fmt.Fprintf
        - fmt.Fprint
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-ST*"
  exclusions:
    rules:
      # Ignoring a write/close error from a test's own fake HTTP handler
      # is normal — a real failure there fails the test via its
      # assertions, not via the ignored return value.
      - path: _test\.go
        linters:
          - errcheck
          - gosec
