# compiled binary
/robotsyes

# local instance config — copy from robotsyes.example.yaml; may name a
# real origin, never commit
/robotsyes.yaml

# scratch
*.log

# calque's own calibration telemetry (fire log for every NEW suspect it
# reports) — not durable memory, unlike .calque/registry.md, which stays
# tracked
/.calque/fires.jsonl

# editor / OS cruft
*.swp
*.swo
*~
.DS_Store
.idea/
.vscode/
